# 移动端 B2C 商城 — 需求分析文档

---

## 1. 项目概述

| 属性 | 说明 |
|------|------|
| 项目名称 | B2C 移动端商城 |
| 项目定位 | 面向 C 端用户的移动电商平台 + 运营后台管理系统 |
| 技术架构 | 前后端分离：uni-app（移动端）+ Vue（管理后台）+ Spring Boot（后端） |
| 开发周期 | 约 1 个月 |

---

## 2. 角色定义

| 角色 | 描述 |
|------|------|
| **游客** | 未登录用户，只能浏览商品（不能加购、下单） |
| **注册用户** | 已登录 C 端用户，可浏览商品、加购、下单、查看订单 |
| **管理员** | 后台运营人员，管理商品、订单、用户 |

---

## 3. 技术选型 & 关键决策

| 决策项 | 方案 |
|--------|------|
| 注册方式 | 手机号验证码 + 邮箱验证码（模拟发送，验证码存 Redis） |
| 库存扣减时机 | **下单时扣减**（提交订单瞬间扣库存） |
| 超卖防护 | **乐观锁**（商品表 version 字段，更新时版本号校验） |
| 购物车存储 | **Redis Hash**（key = `cart:用户ID`，field = 商品ID，value = 数量JSON） |
| 商品缓存 | **Redis String**（热点商品缓存，TTL 过期，更新时清除缓存） |
| 认证方式 | **JWT Token**（登录后签发，请求头携带，拦截器校验） |
| 限时秒杀 | **不做**，专注完整基础流程 |

---

## 4. 功能需求清单

### 4.1 C 端移动端（uni-app）—— 10 个功能模块

#### F1 · 用户注册

| 需求点 | 说明 |
|--------|------|
| 注册方式 | 手机号 + 短信验证码 / 邮箱 + 邮箱验证码，二选一或都支持 |
| 验证码 | 随机 6 位数字，存入 Redis，有效期 5 分钟 |
| 密码 | 6-20 位，BCrypt 加密存储 |
| 校验 | 手机号/邮箱唯一性校验、前后端双重校验 |
| 模拟方式 | 验证码只存 Redis，不真实发送短信/邮件，前端直接展示或通过接口返回（调试用） |

#### F2 · 用户登录

| 需求点 | 说明 |
|--------|------|
| 方式 | 手机号/邮箱 + 密码登录 |
| 认证 | 登录成功返回 JWT Token，有效期 24 小时 |
| 存储 | Token 存入 uni-app 本地缓存，每次请求携带 |
| 拦截 | 后端拦截器校验 Token，未登录返回 401 |

#### F3 · 首页

| 需求点 | 说明 |
|--------|------|
| 轮播图 | 顶部 Banner 轮播（后台可配置） |
| 分类导航 | 商品分类图标入口，点击进入分类商品列表 |
| 商品推荐/列表 | 默认展示商品列表，支持上拉加载分页 |
| 缓存 | 首页商品数据使用 Redis 缓存，TTL 30 分钟 |

#### F4 · 商品搜索

| 需求点 | 说明 |
|--------|------|
| 搜索方式 | 顶部搜索栏，输入关键字模糊搜索商品名称 |
| 分页 | 搜索结果分页展示 |

#### F5 · 商品详情页

| 需求点 | 说明 |
|--------|------|
| 展示内容 | 商品轮播图、商品名称、价格、库存、商品描述 |
| 操作 | 底部「加入购物车」按钮 |
| 校验 | 库存为 0 时按钮置灰，显示「已售罄」 |
| 缓存 | 商品详情 Redis 缓存，TTL 30 分钟 |

#### F6 · 购物车（Redis 实现）

| 需求点 | 说明 |
|--------|------|
| 存储方式 | Redis Hash：`cart:{userId}` → `{skuId: {name, image, price, quantity}}` |
| 操作 | 加入商品（已存在则数量 +1）、修改数量、删除商品、选中/取消选中 |
| 全选 | 一键全选/取消全选 |
| 结算 | 选中商品后点击「去结算」，跳转确认订单页 |
| 登录要求 | 必须登录才能使用购物车 |

#### F7 · 确认订单 & 下单

| 需求点 | 说明 |
|--------|------|
| 页面内容 | 收货地址选择、商品清单、数量、单价、总价 |
| 提交订单 | 生成订单号，状态 =「待付款」，**扣减库存（乐观锁）** |
| 库存扣减 | 使用乐观锁（version 字段），扣减失败提示「下单失败，请重试」 |
| 事务保证 | 订单创建 + 库存扣减 + 清空购物车选中项，同一事务 |

#### F8 · 模拟支付

| 需求点 | 说明 |
|--------|------|
| 方式 | 订单详情页点击「去支付」，直接变更订单状态为「已支付」 |
| 结果 | 跳转支付成功页，订单状态变为「待发货」 |
| 超时处理 | 待付款订单 30 分钟后自动取消，库存回滚（可选，先用定时任务或不做） |

#### F9 · 收货地址管理

| 需求点 | 说明 |
|--------|------|
| 操作 | 新增、编辑、删除、设为默认地址 |
| 字段 | 收货人、手机号、省/市/区、详细地址、是否默认 |

#### F10 · 订单管理

| 需求点 | 说明 |
|--------|------|
| 订单列表 | 按状态 tab 分类：全部 / 待付款 / 待发货 / 待收货 / 已完成 |
| 订单详情 | 订单基本信息 + 商品清单 + 物流信息（发货后显示） |
| 取消订单 | 仅「待付款」状态可取消，取消后库存回滚 |
| 确认收货 | 「待收货」状态可确认收货，状态变为「已完成」 |

### 4.2 后台管理端（Vue + Element Plus）—— 7 个功能模块

#### M1 · 管理员登录

| 需求点 | 说明 |
|--------|------|
| 方式 | 管理员账号密码登录（预设账号，不开放注册） |
| 认证 | JWT Token，与 C 端共用同一认证体系 |

#### M2 · 商品分类管理

| 需求点 | 说明 |
|--------|------|
| 操作 | 分类增删改查 |
| 字段 | 分类名称、排序、状态（启用/禁用） |

#### M3 · 商品管理

| 需求点 | 说明 |
|--------|------|
| 操作 | 商品增删改查、上下架 |
| 字段 | 商品名称、分类、价格、库存、图片（多图上传）、描述、上下架状态 |
| 图片上传 | 后端文件上传接口，存储到本地或 OSS |
| 缓存处理 | 更新商品时清除对应 Redis 缓存 |

#### M4 · 订单管理

| 需求点 | 说明 |
|--------|------|
| 列表 | 按状态筛选查看所有订单 |
| 详情 | 查看订单基本信息 + 商品清单 |

#### M5 · 订单状态流转

| 需求点 | 说明 |
|--------|------|
| 发货 | 「已支付」→「待发货」，管理员填写物流单号 → 状态变为「待收货」 |
| 订单完成 | 用户确认收货后 →「已完成」 |

#### M6 · 用户管理

| 需求点 | 说明 |
|--------|------|
| 列表 | 查看注册用户列表，支持搜索 |
| 操作 | 启用/禁用用户（禁用后该用户无法登录） |

#### M7 · 数据统计看板

| 需求点 | 说明 |
|--------|------|
| 统计项 | 今日订单数、今日营业额、近 7 日订单趋势 |
| 实现 | SQL 聚合查询 GROUP BY 日期 |

---

## 5. 核心业务流程图

### 5.1 C 端购物主流程

```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│  游客浏览  │ → │  用户注册  │ → │  用户登录  │ → │  浏览商品  │
│  商品首页  │   │   / 登录   │   │  (JWT)   │   │  首页/搜索  │
└──────────┘    └──────────┘    └──────────┘    └─────┬────┘
                                                      │
      ┌───────────────────────────────────────────────┘
      ▼
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│  查看商品  │ → │ 加入购物车 │ → │  确认订单  │ → │  提交订单  │
│  详情页    │   │  (Redis)  │   │  选地址    │   │  扣库存    │
└──────────┘    └──────────┘    └──────────┘    └─────┬────┘
                                                      │
      ┌───────────────────────────────────────────────┘
      ▼
┌──────────┐    ┌──────────┐    ┌──────────┐
│  模拟支付  │ → │  待发货    │ → │  确认收货  │
│  改状态    │   │           │   │  已完成    │
└──────────┘    └──────────┘    └──────────┘
```

### 5.2 订单状态流转图

```
                    ┌──────────────────────────────┐
                    │        用户/管理员取消          │
                    │       （仅待付款可取消）         │
                    └──────────────┬───────────────┘
                                   ▼
  ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
  │  待付款    │ → │  已支付    │ → │  待发货    │ → │  待收货    │ → │  已完成    │
  │ (下单后)  │   │ (支付后)  │   │ (同支付)  │   │(管理员发货)│   │(用户确认)│
  └──────────┘    └──────────┘    └──────────┘    └──────────┘    └──────────┘
       ↑
       │  超时 30 分钟自动取消（可选实现）
       │
```

### 5.3 库存扣减乐观锁流程

```
用户下单
   │
   ▼
查询商品库存 & version
   │
   ├── 库存 <= 0 ──→ 返回「库存不足」
   │
   ▼
UPDATE product SET stock = stock - 1, version = version + 1
WHERE id = ? AND version = ? AND stock > 0
   │
   ├── 影响行数 = 0 ──→ 版本冲突，返回「下单失败，请重试」
   │
   ▼
影响行数 = 1 ──→ 扣减成功，创建订单
```

---

## 6. 功能优先级矩阵

| 优先级 | 功能 | 原因 |
|--------|------|------|
| P0·核心链路 | F2登录 → F3首页 → F5详情 → F6购物车 → F7下单 → F8支付 | 电商核心闭环，必须最先做完 |
| P1·重要 | M3商品管理、F9地址管理、F10订单管理、M4/M5订单管理 | 辅助核心链路运转 |
| P2·补充 | F1注册、M1管理员登录、M2分类管理、M6用户管理 | 基础 CRUD，快速完成 |
| P3·拔高 | M7数据看板、F4搜索 | 面经加分项 |

**建议开发顺序**：P0 → P1 → P2 → P3

---

## 7. 待确认事项

| 编号 | 问题 | 待确认 |
|------|------|--------|
| ⚠️ | 注册验证码：你选了「手机号真实验证码」，这需要接入阿里云短信等付费服务。建议改为**模拟验证码**（生成后存 Redis，前端调试模式下直接展示，面试时说明逻辑即可）。是否接受模拟方案？ | 待回复 |

---

> 📌 **下一步**：需求确认后进入「系统设计阶段」→ 数据库表结构设计（ER 图 + DDL SQL）+ 接口文档（API 列表）
